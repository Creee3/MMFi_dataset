# S2P2 Temporal / CMC / Mixup 消融实验说明

## 实验目的

本实验只分析 CMC 与 Mixup 对 S2P2 的影响，包含四组：

| 组别 | CMC | Mixup | 是否重新训练 |
| --- | --- | --- | --- |
| `temporal_only` | 关闭 | 关闭 | 是，运行 seeds 0/1/2 |
| `temporal_cmc` | 开启 | 关闭 | 是，运行 seeds 0/1/2 |
| `temporal_mixup` | 关闭 | 开启 | 是，运行 seeds 0/1/2 |
| `full` | 开启 | 开启 | 否，复用正式 S2P2 结果 |

其中 `temporal_cmc` 直接复用正式实验选出的 `fold02` Teacher checkpoint，不重新训练 Teacher。

## 保持不变的实验口径

- 数据口径：`protocol2 + cross_subject_split`。
- 时间窗口：`L=32`，stride 为 `2`。
- 学生模型：学习率 `0.00020724687211082514`，batch size `128`，seeds 为 `0/1/2`。
- CSI 扰动：noise `0.08`、frequency masking `0.15`、temporal masking `0.15`，三组新实验都保留。
- CMC 正式权重：`lambda_cmc=0.1`。
- Mixup 正式强度：`mixup_alpha=0.2`。
- 每个 seed 仍按照 validation MPJPE 选择最佳 checkpoint，并保持 `--skip_final_test`。

因此，这不是 CSI 扰动消融；三组之间唯一变化是 CMC 与 Mixup 的开关。

## 5090 上的启动命令

下面是 PyCharm 中 Windows CMD 终端可直接使用的命令，不要使用 PowerShell 的 `$env:`、`&` 或 `Tee-Object` 语法。

```bat
cd /d D:\study\four_grade\graduation_article\data_pdf\MMFi_Dataset
C:\Users\CHEN\.conda\envs\bit\python.exe -u .\run_s2p2_temporal_cmc_mixup_ablation.py .\data_base .\config.yaml --num_workers 8 --eval_num_workers 0 --worker_fallbacks 4 2 1 0 --min_free_gpu_mib 0
```

不要改成系统默认的 `python`：当前 5090 上该命令首先指向不可用的 Python 3.14。runner 会自动把项目内的 `_python_deps` 加入自身及训练子进程的依赖路径。

脚本会直接开始，不检查是否剩余 20 GB 显存。训练日志由现有训练代码和 runner 自动保存，不需要额外使用管道或日志命令。

如任务中断，重新执行同一条命令即可。脚本会读取各目录的 `last.pth` 断点续跑，也会跳过已经完整结束的 seed。

## 运行前检查命令

只检查命令与配置、不启动训练：

```bat
C:\Users\CHEN\.conda\envs\bit\python.exe -u .\run_s2p2_temporal_cmc_mixup_ablation.py .\data_base .\config.yaml --num_workers 8 --eval_num_workers 0 --worker_fallbacks 4 2 1 0 --min_free_gpu_mib 0 --dry_run
```

## 输出位置

全部新结果保存在：

```text
strict_offline_runs\S2P2_temporal_cmc_mixup_ablation
```

主要文件：

- `ablation_plan.json`：实验开始前写入的完整配置与来源。
- `<variant>\seed_XX\log.txt`：训练脚本日志。
- `<variant>\seed_XX\process_output.log`：runner 捕获的完整进程输出。
- `<variant>\variant_result.json`：该组 3 个 seeds 的均值与标准差。
- `ablation_result.json`：四组最终对比，其中 `full` 明确标记为复用正式结果。

前三组共需新运行 9 个学生模型，按 `temporal_only -> temporal_cmc -> temporal_mixup` 顺序串行执行。
