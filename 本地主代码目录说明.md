# PrivPose 本地主代码目录说明

完整的环境、训练、消融、评估和断点续跑命令见：

```text
PrivPose代码使用说明_中文.md
```

## 当前活动目录

日常修改、运行实验和向 5090 同步均使用：

```text
/Users/lecha/Desktop/four_grade/graduation_article/data_pdf/MMFi_Dataset
```

对应 5090 路径：

```text
D:\study\four_grade\graduation_article\data_pdf\MMFi_Dataset
```

## 正式 3x3 主线

```text
common.py
config.yaml
mmfi_lib/
run_s2p2_3x3_rank2.py
run_s2p2_cmc_hparam_tuning.py
train_t1_rgb_teacher_mpjpe.py
train_lupi_rgb_teacher_mpjpe.py
```

快速检查和启动：

```bash
./quick_check.sh
SELECTION_FILE=/path/to/selected_for_3x3.json ./run_formal_3x3.sh
```

## S2P2 组件消融

```text
run_s2p2_temporal_cmc_mixup_ablation.py
S2P2消融实验说明.md
```

该 runner 在 `protocol2 + cross_subject_split` 下运行 Temporal only、Temporal + CMC、Temporal + Mixup 三组各 3 个 seeds，并直接复用正式 Full 结果。具体的 5090 CMD 命令、固定配置和输出位置见中文说明文件。

## 其他仍在本地维护的代码

活动目录也继续保留 baseline、独立评估、数据检查、all-zero 回退实验和历史实验脚本。它们没有因为归档而从原目录删除。

## 完整归档

原目录下的完整本地归档：

```text
PrivPose_本地归档_20260815/
```

其中：

- `mainline/`：正式 3x3 完整代码副本；
- `optional_tools/`：baseline、评估、数据检查和回退实验；
- `legacy_scripts/`：历史调参与实验编排；
- `cloud_variants/5090/`：本地和 5090 不同的工具脚本原始版本；
- `归档核验报告_中文.md`：本地与 5090 的哈希核验结果。

活动目录是日常工作的唯一主目录，归档只用于备份和回溯。
