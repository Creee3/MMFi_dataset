# 四模型统一评估维护入口

统一评估适配已放在：

`canonical_eval_s2p2_20260818/`

请先阅读其中的 `README_中文.md`。这份适配只新增评估和核验工具，不修改
PrivPose、MetaFi++、HPE-Li、DT-Pose 的训练代码、模型结构和历史结果。

当前目录默认处理 P2-S2，同时已经适配完整 P1/P2/P3 x S1/S2/S3。九份本地
manifest 均已用真实数据检查；可以按格单独导出、评估和汇总，不要求每次跑满九格。

默认 P2-S2 保持：

- S2P2：`protocol2 / cross_subject_split`
- target index：`62..296`
- 样本数：`24,440`
- CSI 文件检查：`24,440`
- ground-truth 文件检查：`104`

完整九格的样本数和使用方法见目录内中文 README。S1 使用逐动作随机划分，不能
简写成固定 8 个 validation subjects。

适配脚本和本地核验已完成；最终四模型数值尚未伪造或预填。下一步是在 5090 上
用各模型按 validation MPJPE 选择的三个 seed checkpoint 导出预测，再运行统一
评估和汇总。已有格可以先生成部分 3 x 3 表，最终论文表必须通过 9/9 完整性检查。
详细命令及 checkpoint 证据要求见上述中文 README。
