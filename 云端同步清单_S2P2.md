# PrivPose 本地与 5090 同步清单

更新时间：2026-08-15

## 1. 对应目录

本地活动目录：

```text
/Users/lecha/Desktop/four_grade/graduation_article/data_pdf/MMFi_Dataset
```

5090 活动目录：

```text
D:\study\four_grade\graduation_article\data_pdf\MMFi_Dataset
```

日常修改和同步都以本地活动目录为准，不要从归档目录直接覆盖 5090。

## 2. 正式 3x3 必需代码

```text
common.py
config.yaml
mmfi_lib/mmfi.py
mmfi_lib/evaluate.py
run_s2p2_3x3_rank2.py
run_s2p2_cmc_hparam_tuning.py
train_t1_rgb_teacher_mpjpe.py
train_lupi_rgb_teacher_mpjpe.py
```

2026-08-15 已逐文件计算 SHA-256，上述文件的本地版本与 5090 完全一致。

## 3. 仍在活动目录保留的辅助代码

```text
train_baseline_mpjpe.py
run_s2p2_3x3_all_zero.py
check_s2p2_data_integrity.py
evaluate_final_test_mpjpe.py
evaluate_final_test_checkpoints_mpjpe.py
evaluate_final_test_breakdown_mpjpe.py
```

这些文件也与 5090 一致，但不属于正式 3x3 的最小训练依赖。

## 4. 已发现的工具版本差异

### `inspect_s2p2_tuning_logs.py`

本地版本比 5090 版本功能更完整，增加了基于 `history.json` 的进度判断、历史重试错误区分和额外运行时错误识别。保留本地版本作为活动版本，5090 原版已保存在：

```text
PrivPose_本地归档_20260815/cloud_variants/5090/
```

### `rebuild_s2p2_optuna_bookkeeping.py`

本地与 5090 仅有 LF/CRLF 换行差异，逻辑完全一致。

这两个脚本都不参与正式 Teacher/Student 训练。

## 5. 不要作为普通代码同步的目录

```text
data_base/
experiment_results/
strict_offline_runs/
.venv/
_python_deps/
tmp/
rerun_archives/
PrivPose_本地归档_20260815/
资料收集/
```

其中 `strict_offline_runs/` 包含正式 checkpoint 和日志。只有迁移实验结果或续跑时才单独同步，不要与代码更新混在一起覆盖。

## 6. 同步后快速核验

本地：

```bash
./quick_check.sh
```

5090 的 PyCharm/CMD 终端：

```bat
cd /d D:\study\four_grade\graduation_article\data_pdf\MMFi_Dataset
python run_s2p2_3x3_rank2.py --help
python train_lupi_rgb_teacher_mpjpe.py --help
```

正式运行前还要确认：

- `data_base` 路径存在；
- `selected_for_3x3.json` 路径正确；
- `strict_offline_runs/S2P2_3x3_rank2` 中的续跑结果完整；
- 当前环境使用预期的 Conda Python；
- GPU 和磁盘空间满足运行要求。

