# 云端同步清单 S2P2

日期：2026-07-04

本地目录已整理过。根目录现在只保留核心训练/评估代码，旧实验脚本和参考资料已经收纳起来。

## 建议上传到云端的文件

上传这些根目录文件：

```text
common.py
config.yaml
train_baseline_mpjpe.py
train_t1_rgb_teacher_mpjpe.py
train_lupi_rgb_teacher_mpjpe.py
evaluate_final_test_mpjpe.py
evaluate_final_test_breakdown_mpjpe.py
evaluate_final_test_checkpoints_mpjpe.py
run_s2p2_wifi_cmc_mix20.py
run_s2p2_selected_jobs.py
```

上传这个文件夹：

```text
mmfi_lib/
```

如果云端已有 `data_base/`，不用上传本地数据集。

## 不建议上传的内容

这些是资料、旧实验或本地输出，不建议跟训练代码一起传到云端：

```text
_cloud_sync_archive_20260704/
_archived_legacy_20260619/
资料收集/
experiment_results/
strict_offline_runs/
.venv/
.idea/
```

其中 `strict_offline_runs/` 里有 teacher checkpoint。云端如果已经有：

```text
strict_offline_runs\T1_rgb_teacher_single_s2p2_mix02\best.pth
```

就不用重新上传整个 `strict_offline_runs/`。

## 云端上传后先核对

在 Windows 云端目录：

```bat
cd /d D:\study\four_grade\graduation_article\data_pdf\MMFi_Dataset
```

先查文件是否被重复拼接：

```bat
findstr /n "def main" train_lupi_rgb_teacher_mpjpe.py
findstr /n "if __name__" train_lupi_rgb_teacher_mpjpe.py
findstr /n "def get_loaders" common.py
```

正常情况应该大致是：

- `train_lupi_rgb_teacher_mpjpe.py` 只有一个 `def main`
- `train_lupi_rgb_teacher_mpjpe.py` 只有一个 `if __name__ == "__main__"`
- `common.py` 只有一个 `def get_loaders`

再查新版参数是否存在：

```bat
python train_lupi_rgb_teacher_mpjpe.py --help | findstr "skip_final_test lambda_bone eval_num_workers scheduler adaptive_cmc"
```

应该能看到这些参数：

```text
--skip_final_test
--lambda_bone
--eval_num_workers
--scheduler
--adaptive_cmc
```

如果没有，说明云端仍然不是本地新版。

## 推荐下一轮一键命令

等当前云端旧实验停掉并重新上传后，可以跑新版：

```bat
python run_s2p2_wifi_cmc_mix20.py data_base config.yaml --teacher_ckpt strict_offline_runs\T1_rgb_teacher_single_s2p2_mix02\best.pth --epochs 30 --batch_size 64 --val_batch_size 128 --num_workers 2 --eval_num_workers 0 --device cuda --scheduler cosine --adaptive_cmc
```

如果想先只试前 3 个组合：

```bat
python run_s2p2_wifi_cmc_mix20.py data_base config.yaml --teacher_ckpt strict_offline_runs\T1_rgb_teacher_single_s2p2_mix02\best.pth --epochs 30 --batch_size 64 --val_batch_size 128 --num_workers 2 --eval_num_workers 0 --device cuda --scheduler cosine --adaptive_cmc --start 1 --end 3
```

如果只想跑精选组合 `10,14,16,18,19`：

```bat
python run_s2p2_selected_jobs.py data_base config.yaml --teacher_ckpt strict_offline_runs\T1_rgb_teacher_single_s2p2_mix02\best.pth --epochs 30 --batch_size 128 --val_batch_size 256 --num_workers 4 --eval_num_workers 0 --device cuda --scheduler plateau
```

## 当前本地核心文件行数

这些行数可以用来发现云端文件是否异常膨胀：

```text
common.py                           866
train_lupi_rgb_teacher_mpjpe.py     550
train_baseline_mpjpe.py             615
train_t1_rgb_teacher_mpjpe.py       318
run_s2p2_wifi_cmc_mix20.py          475
run_s2p2_selected_jobs.py           194
config.yaml                         328
```
