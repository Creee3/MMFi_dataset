# MetaFi、HPE-Li 与 DT-Pose 调优及正式训练口径记录

更新日期：2026-08-16

本文用于记录三种对比模型的贝叶斯调参来源、正式训练参数、实际生效的 batch size，以及论文结果采用的 checkpoint 选择口径。这里必须区分两件事：

1. **超参数选择**：选择哪一组学习率和 batch size。
2. **checkpoint 选择**：同一次训练中选择哪个 epoch 的结果。

MPJPE 和 PA-MPJPE 都是越低越好，因此应写“最低”或“最优”，不能写“最高”。

## 1. 贝叶斯搜索的共同设置

三种模型使用 `run_bayesian_hparam_tuning_v2.py` 和 `train_pose_wrapper.py` 进行 Optuna TPE 搜索：

- 开发配置：MM-Fi Protocol 2、`cross_subject_split`（P2-S2）。
- 搜索预算：15 个 trial；MetaFi 和 HPE-Li 的归档日志保留了编号 001--014 的 14 个完整候选，DT-Pose 归档保留了 15 个候选。
- 每个 trial：seed 0、1、2，共三次训练。
- 学习率：在 `[1e-4, 5e-3]` 内进行对数均匀搜索。
- batch size 候选：`[16, 32, 64, 128, 256]`。
- 搜索目标：每个 seed 先取最低 PA-MPJPE，再对三个 seed 求平均；Optuna 最小化该平均 PA-MPJPE。
- 搜索脚本本身**不是按 MPJPE 选择超参数**。

调参 wrapper 会正确递增 `step_count`，因此配置 batch 大于单卡 micro-batch 时会执行梯度累积。不过，wrapper 没有将 loss 除以累计步数，累计得到的是梯度之和而不是梯度平均。这会使不同 batch size 的训练动力学不完全可比。

MetaFi 和 HPE-Li 的最终候选都是 batch 16，不需要梯度累积，因此不受这一点影响。DT-Pose 的 trial 008 配置为 batch 32，会受到这一实现细节影响。

## 2. MetaFi++

### 2.1 贝叶斯搜索结果

- 搜索冠军：trial 013。
- 精确学习率：`0.0018450292621694521`，正式记录可写为 `1.85e-3`。
- batch size：`16`。
- 三个 seed 的平均最低 PA-MPJPE：约 `112.57 mm`。

虽然搜索目标是 PA-MPJPE，但重新解析完整 `experiment_log.txt`，并按每个 seed 的最低 MPJPE 对全部 trial 重排后，trial 013 仍然排名第一，平均最低 MPJPE 约为 `235.47 mm`。

因此 MetaFi 的情况是：**PA-MPJPE 搜索冠军恰好也是事后按 MPJPE 重排的冠军**，但不能把搜索脚本描述成“以 MPJPE 为优化目标”。

### 2.2 正式训练参数

- 正式学习率：`1.85e-3`，在 `train_pose*.py` 中硬编码。
- 配置 batch size：`16`。
- 实际优化器更新 batch size：`16`。
- 优化器：SGD，momentum `0.9`。
- 正式 seed：0、1、2、3、4。

正式参数与贝叶斯搜索 trial 013 基本一致。

### 2.3 结果选择口径

原始正式训练代码按 validation PA-MPJPE 保存 checkpoint；论文当前表格则由完整日志重新按 MPJPE 选择：

1. 每个 seed 选择 validation MPJPE 最低的 epoch。
2. 从同一 epoch 读取 PA-MPJPE、PCK@20 和 PCK@50。
3. 对五个 seed 求均值和总体标准差。

因此“调参按 PA-MPJPE”和“论文表格按 MPJPE-selected epoch”可以同时成立，两者不能混写。

## 3. HPE-Li / HPELI

### 3.1 贝叶斯搜索结果

- 搜索冠军：trial 010。
- 精确学习率：`0.0010370844668954541`，约为 `1.04e-3`。
- batch size：`16`。
- 三个 seed 的平均最低 PA-MPJPE：约 `99.96 mm`。

对全部 HPE-Li trial 的 history 事后按每个 seed 的最低 MPJPE 重排后，trial 010 同样排名第一，平均最低 MPJPE 约为 `238.76 mm`。

因此 HPE-Li 也属于：**PA-MPJPE 搜索冠军恰好也是事后 MPJPE 重排冠军**。如果此前记得“有一个模型拿 MPJPE 最优参数去正式跑”，很可能指的是 HPE-Li；但从代码事实看，MetaFi 也具有同样的重合关系。

### 3.2 正式训练参数

- 正式学习率：`1e-3`，在 `train_pose.py` 中硬编码。
- 配置 batch size：`16`。
- 实际优化器更新 batch size：`16`。
- 优化器：SGD，momentum `0.9`。
- 正式 seed：0、1、2、3、4。

正式学习率 `1e-3` 是对贝叶斯候选 `0.001037084...` 的近似取整，二者接近但不完全相同。复现正式论文结果应使用 `1e-3`；复现原始贝叶斯候选时才使用精确值。

### 3.3 结果选择口径

与 MetaFi 相同：原始训练代码按 validation PA-MPJPE 保存 checkpoint，但论文表格由完整日志按每个 seed 的最低 MPJPE epoch 重建，并从同一 epoch 同步读取其他指标。

## 4. DT-Pose

### 4.1 贝叶斯搜索结果

搜索脚本按平均 PA-MPJPE 得出的冠军是：

- trial 009。
- 学习率：`0.0010370844668954541`，约为 `1.04e-3`。
- batch size：`16`。
- 三个 seed 的平均最低 PA-MPJPE：约 `101.85 mm`。

如果事后改按每个 seed 的最低 MPJPE 对全部 trial 重排：

- 第一名：trial 004，`lr=5.95e-4`、`batch=16`，平均 MPJPE 约 `238.70 mm`。
- trial 009：平均 MPJPE 约 `239.41 mm`。
- trial 008：平均 MPJPE 约 `239.66 mm`，排名第 4。

所以 trial 008 既不是 PA-MPJPE 搜索冠军，也不是事后 MPJPE 重排冠军。

### 4.2 正式训练实际使用情况

正式九组 DT-Pose 结果采用了 trial 008 对应的显示参数：

- 学习率：`8.49e-4`。
- YAML 配置 batch size：`32`。
- 单次加载 micro-batch：`16`。
- 原本计划的梯度累计步数：`2`。

但是正式 `train_pose.py`、`train_pose-Copy1.py` 和 `train_pose-Copy2.py` 中，`step_count` 初始化为 0 后没有递增。因此：

- `step_count % steps_per_update == 0` 始终成立。
- 每个 16 样本 mini-batch 后都会立即执行 `optimizer.step()`。
- 梯度累积实际没有生效。
- **真实优化器更新 batch size 是 16，而不是配置显示的 32。**

正式 DT-Pose 的真实训练组合是：

```text
learning rate = 8.49e-4
configured batch size = 32
actual optimizer-update batch size = 16
working gradient accumulation = no
```

这组训练既没有复现 PA-MPJPE 冠军 trial 009，也没有忠实复现 trial 008：

- 与 trial 009 相同的只有实际 batch 16，学习率不同。
- 与 trial 008 相同的是学习率和 YAML 显示值，但 trial 008 在调参 wrapper 中确实执行了两次 micro-batch 累积，正式代码没有。

trial 008 的 `239.66 mm` 是调参 wrapper 正常按配置 batch 32 运行时得到的平均最低 MPJPE，不能把它当作正式代码实际 batch 16 时的结果。正式论文数值必须以三台机器保存的正式日志为准。

目前归档中没有证据解释为什么最终固定选择 trial 008，而不是 `bo_summary.json` 中的 trial 009。只能记录为“正式实验采用了 trial 008 的学习率和配置值”，不能写成“采用贝叶斯搜索得到的最优参数”。

## 5. 三种模型汇总

| 模型 | PA-MPJPE 搜索冠军 | 正式学习率 | 配置 batch | 实际更新 batch | 与搜索冠军关系 |
|---|---:|---:|---:|---:|---|
| MetaFi++ | trial 013：`1.845e-3 / 16` | `1.85e-3` | 16 | 16 | 基本一致；该 trial 事后按 MPJPE 重排也第一 |
| HPE-Li | trial 010：`1.037e-3 / 16` | `1e-3` | 16 | 16 | 学习率近似取整；该 trial 事后按 MPJPE 重排也第一 |
| DT-Pose | trial 009：`1.037e-3 / 16` | `8.49e-4` | 32 | 16 | 不一致；正式代码的梯度累积失效 |

## 6. 后续使用建议

1. 不修改或覆盖已有正式日志，论文结果继续按实际运行记录追溯。
2. 新跑实验前先明确主要指标是 MPJPE 还是 PA-MPJPE，并让超参数选择与 checkpoint 选择使用同一口径。
3. 新版 DT-Pose 若要复现贝叶斯候选，应先修复 `step_count`，并决定累计 loss 使用求和还是除以累计步数取平均。
4. 论文中若只需简要交代，可写“learning rate and batch size were selected on the P2-S2 validation setting”；不要声称三种模型都采用了贝叶斯搜索的全局最优组合。
5. 归档说明中应同时保留“配置参数”和“实际生效参数”，尤其是 DT-Pose 的 batch size。

## 7. 主要证据位置

- MetaFi 搜索完整日志：`rerun_archives/metafi_s2_601_20260811/remote_snapshot/experiment_log.txt`
- MetaFi 正式代码：`rerun_archives/metafi_s2_601_20260811/remote_snapshot/train_pose.py`
- HPE-Li 搜索汇总：`rerun_archives/hpeli_792_20260811/remote_snapshot/experiment_results/bayesian_hparam_tuning/hpeli/bo_summary.json`
- HPE-Li trial histories：`rerun_archives/hpeli_792_20260811/remote_snapshot/experiment_results/bayesian_hparam_tuning/hpeli/bo_trials/`
- HPE-Li 正式代码：`rerun_archives/hpeli_792_20260811/remote_snapshot/train_pose.py`
- DT-Pose 搜索汇总：`rerun_archives/dtpose_823_20260815/remote_snapshot/experiment_results/bayesian_hparam_tuning/dtpose/bo_summary.json`
- DT-Pose trial histories：`rerun_archives/dtpose_823_20260815/remote_snapshot/experiment_results/bayesian_hparam_tuning/dtpose/bo_trials/`
- DT-Pose 正式代码代表：`rerun_archives/dtpose_762_20260816/remote_snapshot/train_pose.py`
- 贝叶斯搜索入口：各归档中的 `run_bayesian_hparam_tuning_v2.py`
- 贝叶斯训练 wrapper：各归档中的 `train_pose_wrapper.py`
