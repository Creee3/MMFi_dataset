# Preserve byte-identical files covered by the source-integrity manifests.
s2p2_equal_samples_mpjpe_code_20260820/** -text

# Keep Windows launchers in their tested on-cloud representation.
launchers/*.cmd -text
*.bat -text

# Preserve the archived DT-Pose/HPE-Li source bytes used for integrity checks.
# Its upstream CSV/Windows files intentionally contain CRLF and trailing spaces.
models/dtpose/official_source/** -text -whitespace
