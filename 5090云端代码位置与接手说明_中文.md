# 5090 云端代码位置与接手说明

更新日期：2026-08-16

这份文件用于后续同学接手 PrivPose/MM-Fi 工程。云端账号、SSH 密码和 Tailscale 权限不写入代码目录，请向项目负责人或机器管理员单独获取。

## 1. 唯一活动代码目录

学校 5090 上当前使用的工程根目录：

```text
D:\study\four_grade\graduation_article\data_pdf\MMFi_Dataset
```

日常运行、修改和同步都应在这个目录完成。不要把归档目录当作活动代码目录，也不要在其他盘重新建立同名工程后混用结果。

进入目录：

```bat
cd /d D:\study\four_grade\graduation_article\data_pdf\MMFi_Dataset
```

## 2. 目录结构

```text
MMFi_Dataset
├── data_base                              MM-Fi 数据，体积很大
├── strict_offline_runs                    正式训练、消融、checkpoint 和日志
├── mmfi_lib                               数据读取与评估代码
├── _python_deps                           5090 补充 Python 依赖
├── _archived_python_20260816              云端历史 Python 文件归档
├── common.py                              模型与公共训练逻辑
├── config.yaml                            协议、数据划分和模态配置
├── run_s2p2_3x3_rank2.py                  正式 3×3 runner
├── run_s2p2_temporal_cmc_mixup_ablation.py 当前组件消融 runner
├── train_t1_rgb_teacher_mpjpe.py          Teacher 训练
└── train_lupi_rgb_teacher_mpjpe.py        Student/CMC/Mixup 训练
```

## 3. 重要结果位置

正式 3×3：

```text
strict_offline_runs\S2P2_3x3_rank2
```

S2P2 Temporal/CMC/Mixup 消融：

```text
strict_offline_runs\S2P2_temporal_cmc_mixup_ablation
```

正式学习率与 batch size 选择文件：

```text
strict_offline_runs\S2P2_cmc_lr_bs_tuning_w84200_resume_clean2\selected_for_3x3.json
```

不要删除 `strict_offline_runs`。其中不仅有日志，还包含 `best.pth`、`last.pth`、Teacher checkpoint、实验计划和论文结果来源。

## 4. 正确 Python 环境

当前确认可用的 Python：

```text
C:\Users\CHEN\.conda\envs\bit\python.exe
```

系统默认 `python` 可能指向不可用的 Python 3.14，不要直接使用。

在 Windows CMD 中初始化：

```bat
cd /d D:\study\four_grade\graduation_article\data_pdf\MMFi_Dataset
set "PYTHONPATH=%CD%\_python_deps"
set "PYTHONNOUSERSITE=1"
C:\Users\CHEN\.conda\envs\bit\python.exe -c "import torch, scipy, yaml, numpy; print(torch.cuda.is_available())"
```

输出 `True` 表示 CUDA 可用。OpenCV 的 depth 警告不影响当前 WiFi CSI、2-D pose 和 3-D ground-truth 主线。

## 5. 查看机器和任务状态

GPU：

```bat
nvidia-smi
```

查看所有 Python 任务及完整命令：

```bat
wmic process where "name='python.exe'" get ProcessId,ParentProcessId,CommandLine /format:list
```

查看某个训练日志末尾，先把路径替换为实际 seed：

```bat
powershell -NoProfile -Command "Get-Content -LiteralPath 'D:\study\four_grade\graduation_article\data_pdf\MMFi_Dataset\strict_offline_runs\S2P2_temporal_cmc_mixup_ablation\temporal_cmc\seed_00\log.txt' -Tail 30"
```

验证阶段可能几分钟没有新日志且 GPU 利用率较低。只要训练主进程和 worker 仍存在，就不要立即判断为卡死。

## 6. 当前 S2P2 消融的启动与续跑

```bat
C:\Users\CHEN\.conda\envs\bit\python.exe -u .\run_s2p2_temporal_cmc_mixup_ablation.py .\data_base .\config.yaml --num_workers 8 --eval_num_workers 0 --worker_fallbacks 4 2 1 0 --min_free_gpu_mib 0
```

同一命令同时用于首次启动和断点续跑。脚本会：

- 跳过已有 `train_complete.json` 的 seed；
- 从 `last.pth` 继续未完成任务；
- 依次运行 Temporal only、Temporal + CMC、Temporal + Mixup；
- 直接复用正式 Full 结果，不重新训练 Full；
- 直接复用正式 `fold02` Teacher，不重新训练 Teacher。

最终结果：

```text
strict_offline_runs\S2P2_temporal_cmc_mixup_ablation\ablation_result.json
```

## 7. 正式 3×3 runner

正式入口：

```bat
C:\Users\CHEN\.conda\envs\bit\python.exe -u .\run_s2p2_3x3_rank2.py .\data_base .\config.yaml --num_workers 8 --eval_num_workers 0 --worker_fallbacks 4 2 1 0 --min_free_gpu_mib 0
```

现有正式 3×3 已经完成。除非明确需要补跑或恢复缺失单元，否则不要随意重新启动，更不要更换 `results_root` 后把新旧结果混在论文中。

## 8. 独立评估

示例：评估正式 S2P2 seed 0 的 `best.pth`：

```bat
C:\Users\CHEN\.conda\envs\bit\python.exe -u .\evaluate_final_test_mpjpe.py .\data_base .\config.yaml --run_dir .\strict_offline_runs\S2P2_3x3_rank2\protocol2\cross_subject_split\student\seed_00 --model_type lupi --checkpoint best.pth --num_workers 0
```

训练过程中的 validation 与独立 final test 必须区分。checkpoint 当前按 validation MPJPE 选择。

## 9. 云端代码归档

不再作为主线使用的旧 Python 文件保存在：

```text
_archived_python_20260816
```

其中：

- `historical_experiments/`：旧实验编排脚本；
- `maintenance_tools/`：环境诊断与 Optuna 维护工具；
- `accidental_empty_files/`：早期误生成的 0 字节文件；
- `归档说明_中文.md`：详细归档清单。

归档文件没有删除，但运行旧脚本前必须先检查相对路径和输出目录。当前活动代码始终以项目根目录为准。

## 10. 接手机器或迁移时必须保存

代码最小集合：

```text
common.py
config.yaml
mmfi_lib/
run_s2p2_3x3_rank2.py
run_s2p2_cmc_hparam_tuning.py
run_s2p2_temporal_cmc_mixup_ablation.py
train_t1_rgb_teacher_mpjpe.py
train_lupi_rgb_teacher_mpjpe.py
evaluate_final_test_mpjpe.py
```

实验复现还必须保存：

```text
strict_offline_runs\S2P2_3x3_rank2
strict_offline_runs\S2P2_temporal_cmc_mixup_ablation
strict_offline_runs\S2P2_cmc_lr_bs_tuning_w84200_resume_clean2\selected_for_3x3.json
```

`data_base` 体积很大，应单独迁移并核验目录结构。不要只保存 Python 文件后就认为实验可以复现。

## 11. 禁止事项

- 不要删除或重命名 `data_base`、`strict_offline_runs`、`mmfi_lib`。
- 不要在 runner 运行期间移动主线 Python 文件。
- 不要使用系统默认 `python`。
- 不要把 validation 指标写成 official test performance。
- 不要用 all-zero 或旧历史脚本替代正式 S2P2 结果。
- 不要在 Markdown、代码或日志中保存 SSH 密码和其他账号凭据。
- 不要覆盖已有正式结果目录；新实验必须使用独立目录。

## 12. 进一步阅读

项目根目录还应包含：

```text
PrivPose代码使用说明_中文.md
S2P2消融实验说明.md
本地主代码目录说明.md
```

其中 `PrivPose代码使用说明_中文.md` 提供更完整的训练、消融、评估和输出文件说明。
