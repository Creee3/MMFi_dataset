# 5090 云端四模型目录说明

更新时间：2026-08-18

## 1. 总原则

`D:\study\four_grade\graduation_article\data_pdf\MMFi_Dataset` 是云端唯一活动根目录。

- 根目录保留 PrivPose 的主入口和正式运行依赖。
- `models\` 只用于按模型查看代码入口，不复制或改写源码。
- 旧路径暂时保留为兼容入口，已有命令不需要立即改写。
- `data_base`、`strict_offline_runs`、`rerun_archives` 和 `_python_deps` 是共享资源，不能随意移动或删除。

## 2. 四个模型

| 模型 | 主入口或源码位置 | 整理后的查看入口 | 说明 |
| --- | --- | --- | --- |
| PrivPose | 根目录的 `run_s2p2_3x3_rank2.py`、`common.py`、`mmfi_lib\` 等 | `models\privpose\`（说明入口）；实际主线仍在根目录 | 根目录主线，不要把核心文件移出根目录 |
| MetaFi++ | `metafi_s2p2_cloud_bundle_20260817\` | `models\metafi_pp\code\` | `code` 是目录联接，指向原始活动包 |
| DT-Pose | `models\dtpose\official_source\`（官方 DT-Pose 基础工程） | `models\dtpose\code\` | 逐关节评估和 DT-Pose 训练共用这份基础工程 |
| HPE-Li | `models\dtpose\official_source\model\hpeli\`，由共享 `train_pose.py` 调用 | `models\hpeli\code\` | HPE-Li 不是独立复制的工程；与 DT-Pose 共用基础工程的 feeder、配置和运行框架 |

`models\metafi_pp\code`、`models\dtpose\code` 和 `models\hpeli\code` 当前是 Windows 目录联接，不是第二份代码。修改时只认它们指向的真实目录，避免出现两份源码不一致。

为了兼容旧命令，根目录的 `source\` 也保留为指向 `models\dtpose\official_source\` 的目录联接。

## 3. PrivPose 正式入口

在根目录运行：

```bat
cd /d D:\study\four_grade\graduation_article\data_pdf\MMFi_Dataset
C:\Users\CHEN\.conda\envs\bit\python.exe -u .\run_s2p2_3x3_rank2.py .\data_base .\config.yaml --num_workers 8 --eval_num_workers 0 --worker_fallbacks 4 2 1 0 --min_free_gpu_mib 0
```

正式结果位于：

```text
strict_offline_runs\S2P2_3x3_rank2\
```

## 4. 对比模型与评估

MetaFi++ 的 S2P2 重训入口：

```text
models\metafi_pp\code\cloud_rerun\run_metafi_s2p2_three_seeds.py
```

HPE-Li 与 DT-Pose 的归档 checkpoint：

```text
rerun_archives\hpeli_792_20260811\s2p2_best_checkpoints\
rerun_archives\dtpose_762_20260816\s2p2_best_checkpoints\
```

分类目录下也提供了不复制文件的快捷入口：

```text
models\hpeli\checkpoints\
models\dtpose\checkpoints\
```

它们分别指向上面的两个归档目录。

逐关节评估工程现在归类在：

```text
evaluations\per_joint\p2s2_baseline_20260816\
```

其中的 `source\` 是评估所需的完整共享源码。根目录的 `p2s2_per_joint_baseline_rerun_20260816\` 仍保留为兼容联接，旧命令无需改写。

## 5. 路径规则

1. 新实验使用新的结果目录，不覆盖已有 `strict_offline_runs\` 结果。
2. 运行 HPE-Li/DT-Pose 旧代码时，逐关节评估命令继续使用兼容路径即可；不要把 `models\hpeli\code` 当成独立完整工程。
3. MetaFi++ 的命令可以继续使用旧路径 `metafi_s2p2_cloud_bundle_20260817\`；该路径在最终收拢前保持有效。
4. 不要删除目录联接目标；删除联接本身也不会删除目标源码，但会使旧命令失效。
5. `source` 中包含官方 DT-Pose 框架以及 HPE-Li/MetaFi 的模型实现，按共享依赖保留，不拆成互相复制的版本。

## 6. 当前整理状态

- 已建立 `models\` 分类入口。
- 已建立 MetaFi++、DT-Pose、HPE-Li 的代码目录联接。
- 已将 DT-Pose 共享源码和逐关节评估工程收拢到 `models\`、`evaluations\` 下，并保留旧路径联接。
- 未修改模型代码、配置、checkpoint 或训练结果。
- MetaFi++ 的 7.5 GB 实体包暂留在根目录原路径；`models\metafi_pp\code\` 已验证可用。由于 Windows 外部程序仍持有该包的文件锁，本轮不强行移动缓存，避免影响后续复现。
